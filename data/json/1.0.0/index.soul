// Built-in Json library wrapping json module
static sanctuary Json {
    soul parse(jsonString) {
        return json.decode(jsonString)
    }
    
    soul stringify(object) {
        return json.encode(object)
    }
    
    soul decode(jsonString) {
        return json.decode(jsonString)
    }
    
    soul encode(object) {
        return json.encode(object)
    }
}