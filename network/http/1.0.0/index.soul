// Built-in Http library wrapping http module
static sanctuary Http {
    soul handle(path, callback) {
        return http.handle(path, callback)
    }
    
    soul listen(port, callback) {
        return http.listen(port, callback)
    }
}